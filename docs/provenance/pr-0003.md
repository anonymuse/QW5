# Pull request 0003 AI provenance

- **Date and pull request:** 2026-07-14;
  [draft pull request #3](https://github.com/anonymuse/QW5/pull/3) on
  `codex/m1-contracts-and-measurement-plan`.
- **Coding surface and model family:** Codex desktop; model family not verifiable from
  the task context.
- **Reasoning setting:** Not verifiable from the task context.
- **Task objective:** Complete the remaining M0 contract freeze assigned by
  `PROJECT_HANDOFF.md`, then define the reviewed architecture contracts, measurement
  design, evidence gates, and ordered decomposition required to execute M1 safely
  without beginning M1 implementation or cluster work.
- **Material human direction:** The project owner required verification of the QW5
  repository and merged foundation, single-agent Sol ownership of architecture and
  integration, strict evidence-class separation, complete hardware/link/model/tensor/
  placement contracts, Terra/Luna-ready bounded follow-ups, no model or cluster
  execution, and one draft PR. After PR #2 removed the immutable PR #1 provenance
  file, the owner authorized a one-time exception to proceed without recreating or
  rewriting that merged record and required future per-PR discipline. PR review then
  required the milestone boundary to match the handoff; canonical exact bytes; schema
  plus semantic validation; exhaustive hostile vectors; an auditable TB5 plan/wire/
  synchronization/result chain; repeated memory baselines; and Sol-owned
  classification and pre-analysis decisions before bounded Terra implementation.
  A second-pass architecture review then required raw-to-summary digest resolution,
  placement lineage/applicability, an explicit synchronization-semantics choice,
  deterministic M1-03 route/stop/buffer controls, and a frozen SafeTensors parser
  profile. The owner selected empirical synchronization option 2 and directed the PR
  to remain draft pending independent re-review. That re-review required one focused
  contract amendment for an acyclic TB5 plan/evidence relationship, resolver-only
  placement `GO`, exact non-pooled thermal/Low Power Mode/power-source regimes,
  complete end-to-end evidence resolution, role-derived model completeness, and
  SafeTensors metadata/canonical-profile validation. On 2026-07-16 the owner confirmed
  that `coordinator_observed_window_v1` is solely an empirical attempt-inclusion score
  and never an actual-start estimate, start-skew bound, clock proof, or one-way-delay
  claim, and limited “refactor” to this amendment and its regression tests.
- **Files generated or materially changed:** Milestone and 16-task decomposition;
  canonical JSON, hardware, TB5 measurement/wire, model/tensor, and placement contract
  specifications; four ADRs; 16 JSON Schemas; 16 positive fixtures; 87 hostile
  mutations; exact canonical, wire, TB5-evidence, and SafeTensors vectors; versioned
  semantic validator and pinned validation-only dependencies; CI coverage; active-
  board state; and this record. The focused review amendment added the immutable model
  acquisition plan and placement evidence graph schemas/fixtures and revised only
  their directly related TB5, model, placement, SafeTensors, coordination, ADR, and
  provenance surfaces.
- **Tests and checks performed:** JSON parsing; Draft 2020-12 meta-schema validation;
  schema and semantic validation of 16 committed positive fixtures, one generated
  acyclic 108-control/246-cell plan-to-summary evidence graph, and model-acquisition/
  manifest plus placement-graph/analysis bundle invariants; rejection of 87 committed
  hostile mutations plus all-complete/zero-sample, raw-disagreement, cyclic-plan,
  unresolved-placement-graph, and producer-asserted-`GO` bundle hostiles;
  independent reproduction of canonical, wire, TB5-evidence, and SafeTensors
  bytes/digests/projections;
  relative Markdown and referenced external-primary-source link checks; Zig 0.16.0
  formatting, build, unit, smoke, and
  inventory checks; `git diff --check`; commit-trailer verification; and focused scans
  for unsupported claims, mixed evidence, credentials, private paths and machine
  identifiers, attribution, licensing, and implementation-source adaptation.
- **Review process:** One coherent architecture task with no delegated or parallel
  writers. Architecture and contract review compared the complete change set with
  `PROJECT_HANDOFF.md`, ADRs 0001–0003, benchmark methodology, hardware topology,
  coordination policy, two rounds of reviewer-supplied adversarial examples, the
  owner's synchronization-semantics decision, and cited primary specifications. The
  focused amendment was traced to the six open second-pass findings and regression-
  tested without general cleanup or M1 implementation. The final diff was reviewed
  for scope, evidence lineage, negative results, public safety, and non-goals before
  publication.
- **Dependencies and external references:** Zig 0.16.0 and its standard library;
  JSON Schema draft 2020-12; NIST FIPS 180-4; RFC 8259; RFC 9293; Unicode UAX #15;
  Apple Foundation storage, power, and thermal API documentation; Apple Network
  framework route-selection/path APIs; Hugging Face Hub immutable-revision download
  documentation; the SafeTensors format description pinned to commit
  `6eb4dc9a28ebce297606e0f4836bbf28839cacef`; and
  the official Qwen source revisions already pinned by ADR-0001.
  `jsonschema==4.25.1` and its exact transitive versions are pinned development/CI
  dependencies, not production runtime dependencies; their installed package metadata
  reports MIT licensing.
- **Adapted non-AI-authored source:** None. No implementation source was copied or
  adapted. External specifications and reference-format documentation were studied
  and attributed; schema and prose content are original QW5 material.
- **Human interventions or decisions:** The owner selected the thesis, model sequence,
  hardware inputs, task scope, model-routing boundary, publication workflow, ban on
  Ultra/parallel agents, current no-execution boundaries, and the one-time historical
  provenance exception, and the exact empirical coordinator-observed inclusion
  limitation recorded above. The owner must still approve ADRs 0004–0007 as a merged
  contract, physical
  protocol plans/access, model acquisition, headroom/reserve policy, quantization
  candidates, text-subset rules, and post-M1 next steps.
